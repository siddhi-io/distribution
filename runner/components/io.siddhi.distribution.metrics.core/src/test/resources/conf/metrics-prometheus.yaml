# Configuration file to test metrics with prometheus reporter

# Carbon Metrics Configuration Parameters
metrics.prometheus:
  reporting:
    prometheus:
      - name: prometheus
        enabled: true
        pollingPeriod: 600
